[build]
builder = "dockerfile"

[deploy]
# "never" — don't auto-restart when the scrape finishes normally.
# If it crashes mid-run, redeploy manually; the checkpoint will resume.
restartPolicyType = "never"
